#ifndef __PARSER_HPP__
# define __PARSER_HPP__

class Parser
{
	public:
		Parser( void ); // -------------------------------------------- CANONICAL
		~Parser( void );
		Parser( const Parser& );
		Parser& operator=( const Parser& );
	private:
		// Needs definitions
};

#endif /* __PARSER_HPP__ */

