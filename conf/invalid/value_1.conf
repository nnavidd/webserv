
autoindex			on;
autoindex			not-valid;			# invalid value

server
	port			80;
	server_name		example.com;
	root			/var/www;
	location
		root   /example;
		uri    /;
		method GET;
		autoindex on;
		cgi idontknow;
